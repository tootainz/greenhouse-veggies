// Module for storing application wide constants so they can be changed easily in one place

export const SVG_1_WIDTH = window.innerWidth/2;
export const SVG_1_HEIGHT = window.innerHeight*0.75;
export const MAIN_GRAPH_WIDTH = SVG_1_WIDTH*0.65;
export const MAIN_GRAPH_HEIGHT = SVG_1_HEIGHT*0.65;

export const SVG_2_WIDTH = window.innerWidth*0.35;
export const SVG_2_HEIGHT = window.innerHeight*0.6;

export const SVG_BG_WIDTH = window.innerWidth*0.5;
export const SVG_BG_HEIGHT = window.innerHeight;

export const VEG_GRAPH_WIDTH = SVG_2_WIDTH*0.8;
export const VEG_GRAPH_HEIGHT = SVG_2_HEIGHT*0.6;

export const TEMP_GRAPH_HEIGHT = VEG_GRAPH_HEIGHT*0.45;

export const VEG_GRAPH_OFFSET = 30;